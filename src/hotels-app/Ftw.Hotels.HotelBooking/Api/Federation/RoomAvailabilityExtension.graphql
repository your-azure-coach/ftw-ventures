extend type Room {
	availability(numberOfDays: Int = 7, exceptionPercentage: Int = 0): [RoomAvailability]! @delegate(path: "roomAvailability(roomId: $fields:id, numberOfDays: $arguments:numberOfDays, exceptionPercentage: $arguments:exceptionPercentage )")
}